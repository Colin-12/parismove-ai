# Requirements pour Streamlit Cloud (point d'entrée racine).
# Ce fichier est lu automatiquement par Streamlit Cloud lors du déploiement.
# Il pointe vers le dashboard et installe ses dépendances + packages internes.

streamlit>=1.36
streamlit-folium>=0.20
folium>=0.16
plotly>=5.20
pandas>=2.2
psycopg[binary]>=3.2
sqlalchemy>=2.0
pydantic>=2.5
pydantic-settings>=2.1
httpx>=0.27
groq>=0.11
tenacity>=8.2

# Packages internes en mode editable
-e ./shared
-e ./services/ingestion
-e ./services/healthscore
-e ./services/coach
-e ./services/dashboard
